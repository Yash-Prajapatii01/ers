// import '../models/sections.dart';
// import '../models/udfModel.dart';
//
// /// 2️⃣ UdfExtractor
// /// Given a SectionModel, returns its UDFs.
// class UdfExtractor {
//   /// Simply returns the nested list — you already have them on SectionModel
//   List<UdfModel> extractUdfs(SectionModel section) => section.udfs;
//
//   // /// If you need ordering/filtering, add it here:
//   // List<UdfModel> extractRequiredSortedByOrder(SectionModel section) {
//   //   final requiredUdFs =
//   //   section.udfs.where((udf) => udf.isRequired).toList();
//   //   requiredUdFs.sort((a, b) => a.order.compareTo(b.order));
//   //   return requiredUdFs;
//   // }
// }